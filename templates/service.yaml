apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmy.fullname" . }}
  labels:
    {{- include "helmy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmy.selectorLabels" . | nindent 4 }}
